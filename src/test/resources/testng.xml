<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<!--
  ═══════════════════════════════════════════════════════════════════════
  EcoPOS E2E Test Suite
  ═══════════════════════════════════════════════════════════════════════

  Chạy: mvn test
  Chỉ E2E: mvn test -Dtest=E2eSuiteTest
  Chỉ FNB: mvn test -Dsuite=fnb (tùy chỉnh trong pom.xml)
  ═══════════════════════════════════════════════════════════════════════
-->

<suite name="EcoPOS E2E Test Suite" verbose="1" parallel="false">

    <!-- ═══ E2E: Toàn bộ kiểm thử đơn hàng (55 TCs) ═══ -->
    <test name="E2E Order Tests">
        <classes>
            <class name="com.ecopos.e2e.E2eSuiteTest"/>
        </classes>
    </test>

    <!-- ═══ Mobile: Online Order UI (tắt mặc định, bật khi có device) ═══ -->
    <!--
    <test name="Mobile Online Order Tests" enabled="false">
        <classes>
            <class name="com.ecopos.mobile.OnlineOrderMobileTest"/>
        </classes>
    </test>
    -->

    <!-- ═══ FNB: Food management (tắt mặc định, bật khi cần) ═══ -->
    <!--
    <test name="FNB Tests" enabled="false">
        <classes>
            <class name="com.ecopos.fnb.CreateFoodTest"/>
            <class name="com.ecopos.fnb.CreateSideFoodTest"/>
            <class name="com.ecopos.fnb.EditFoodTest"/>
            <class name="com.ecopos.fnb.DeleteFoodTest"/>
        </classes>
    </test>
    -->

</suite>
